<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projeto.admcond">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Para que o firebase saiba o estado da conexão do usuario -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".cadastro.CadastroNovaLeitura"
            android:label="Nova leitura" />
        <activity
            android:name=".DataConsumoActivity"
            android:label="Adicionar leitura" />
        <activity
            android:name=".SuporteActivity"
            android:label="Suporte" />
        <activity
            android:name=".TipoConsumo"
            android:label="Tipo de consumo" />
        <activity
            android:name=".cadastro.CadastroCondominio"
            android:label="Cadastro condomínio" />
        <activity
            android:name=".DetalhesInquilinosActivity"
            android:label="Consumo inquílino" />
        <activity
            android:name=".configfirebase.ResetSenha"
            android:label="Redefinição de senha" />
        <activity
            android:name=".MoradoresActivity"
            android:label="Moradores" />
        <activity
            android:name=".RelatorioActivity"
            android:label="Relatórios" />
        <activity
            android:name=".cadastro.CadastroMorador"
            android:label="Adicionar morador" />
        <activity
            android:name=".cadastro.CadastroUsuario"
            android:label="Cadastro síndico" />
        <activity
            android:name=".MainActivity"
            android:label="Menu principal" />
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>